# V0.4.0-rc1 — Diff / Replay / Versioning E2E Review

## Integrated

- fixed the demo package dependency contract for the V0.4 release line;
- promoted framework and demo package versions to `0.4.0rc1`;
- added `demo.versioned_ndjson` as the seventh reference job;
- added deterministic V1/V2 fixtures and keyed diff proof;
- added content-addressed version creation and atomic published-pointer proof;
- added strict replay verification from historical RAW;
- added an integration test for the complete lifecycle;
- extended clean-wheel smoke validation to V0.4 RC behavior;
- made successful CI publish the RC1 source artifact.

## Non-regression

The six pre-existing reference jobs remain present and are re-executed from clean wheels.
V0.1/V0.2/V0.3 APIs are not removed by this milestone.

## Release boundary

RC1 is a qualification milestone. The final `V0.4.0` release must only harden, document and
promote this behavior; it must not add new features.
