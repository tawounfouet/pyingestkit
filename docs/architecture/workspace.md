# Workspace

The default workspace is `.pyingest/` with `state/`, `logs/`, `runs/`, and `published/` roots. Run artifacts remain namespaced by job ID and UUID. Plugins never silently override the global workspace.
